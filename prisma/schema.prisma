generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model notification {
  id       String   @id @map("_id") @db.ObjectId
  text     String
  link     String
  tipo     String
  createAt DateTime
  check    Boolean
}
